#' Visualise results of node-splitting analysis
#' 
#' Pending, but could be useful for getting an overview.
#' 
#' @param nodesplitResults tidy data frame with consistency, direct and indirect HR estimates with CrIs. 

plotNodesplitResults <- function(nodesplitResults) {
	print("Pending.")
}